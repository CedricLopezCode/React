import React from "react";

const recupParamUrl = (props) => {
    return <h1 className = "border border-dark p-2 m-2 text-white text-center bg-primary rounded">
        <h3>{this.props.id}</h3>
        <h3>{this.props.deuxparam}</h3>
    </h1>
};

export default recupParamUrl;